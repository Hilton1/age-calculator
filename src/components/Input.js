import styled from 'styled-components';

export default styled.input`
  width: 164px;
  height: px;

  border: 2px solid hsl(0, 0%, 86%);
  border-radius: 8px;
  outline: none;

  appearance: none;

  padding: 8px 16px;
  font-weight: bold;
  font-size: 32px;
`;
